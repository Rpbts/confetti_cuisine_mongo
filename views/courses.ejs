<!DOCTYPE html>
<Html>

<head>
    <title>
        COURSES
    </title>

</head>

<body>
<h1>Our Courses</h1>
<% offeredCourses.forEach(course => { %>
    <h5><%= course.title %></h5>
    <span><%= course.cost %> </span>
 <% }); %>
 
</body>

</Html>